// Global Mobile Breakpoints (in pixels)

export const BREAKPOINTS = {
  MOBILE_SM: 500, // For tight spaces like header stacking or extreme mobile constraints
  MOBILE_LG: 650, // The breakpoint for timeline switching to single column
  TABLET: 768,    // General mobile vs desktop layout switch (Header, etc.)
  DESKTOP_SM: 850 // Small desktop adjustments (e.g. adding duration text)
};
